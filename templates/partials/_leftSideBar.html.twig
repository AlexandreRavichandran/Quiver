<nav>
    <ul class="text-center md:flex xl:block">
        <a class="block text-gray-400" href="#"><i class="bi bi-plus text-gray-400"></i> Créer un espace</a>
        {% for space in spaces %}
        <li class="list-item p-3 flex justify-center">
            <a class="block" href="{{path('app_space_show',{'id':space.id})}}">
                <img class="inline" src="https://placehold.it/25x25" alt="">
               {{space.name}}
            </a>
        </li>
        {% endfor %}
        <a class="block p-3" href="{{path('app_space_index')}}"><i class="bi bi-compass"></i> Découvrir des espaces</a>
    </ul>

</nav>

