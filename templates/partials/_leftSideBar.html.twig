<nav>
    <ul class="text-center md:flex xl:block">
        <a class="block text-gray-400 modal-open" href="#"><i class="bi bi-plus text-gray-400"></i> Créer un espace</a>
        {% include 'partials/modals/_space_create_modal.html.twig' %}
        {% for space in spaces %}
            <li class="list-item p-3 flex justify-center hover:bg-gray-200">
                <a class="block" href="{{path('app_space_show',{'id':space.id})}}">
                <div class="grid grid-cols-12 pt-3">
                    <div class="col-span-12 lg:col-span-6 lg:flex lg:justify-end">
                        <img class="inline" src="https://placehold.it/25x25" alt="">
                    </div>
                    <div class="col-span-12 lg:ml-2 lg:col-span-6 lg:flex lg:justify-start">
                        {{space.name}}
                    </div>
                </div>
                </a>
            </li>
        {% endfor %}
        <a class="block p-3" href="{{path('app_space_index')}}#discoverSpaces"><i class="bi bi-compass"></i> Découvrir des espaces</a>
    </ul>
</nav>

