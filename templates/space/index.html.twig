{% extends 'base.html.twig' %}

{% block title %}Espaces{% endblock %}

{% block body %}
    <div class="container mx-auto p-4">
        <div class="grid grid-cols-10 gap-4 flex justify-between">
            <div class="col-span-12 sm:col-span-12 lg:col-span-8 xl:col-span-6 p-4 bg-white border border-gray-300">
                <div class="spaceHeader flex justify-between">
                    <h1 class="font-black text-2xl">Vos espaces</h1>
                    <select id="orderSpaces">
                        <option value="name">Ordre alphabetique</option>
                        <option value="lastVisited">Visité récemment</option>
                    </select>
                </div>
                <span class="flex justify-start mt-2">
                    <a href="#" class="modal-open block p-2 rounded border mr-4"><i class="bi bi-plus-circle mr-1"></i>Créer un espace</a>
                    {% include 'partials/modals/_space_create_modal.html.twig' %}
                    <a href="#discoverSpaces" class="block p-2 rounded border"><i class="bi bi-compass mr-1"></i>Découvrir des espaces</a>
                </span>
                <div class="userSpaceList">
                {% include 'space/partials/_userSpaceList.html.twig' %}
                </div>
            </div> 
        </div>
        <div class="grid grid-cols-12 gap-4 flex justify-between">
            <div class="col-span-12 lg:col-span-10 xl:col-span-10 p-4 ">
                <h2 class="font-black text-2xl"> Découvrir des espaces </h2>
                <div class="grid grid-cols-12 gap-4 mt-4 spaceList flex flex-wrap mb-3">
                {% for space in spaces %}
                    <div data-id="{{space.id}}" class="col-span-12 md:col-span-6 lg:col-span-4 spaceCard bg-white rounded-xl p-2 border border-gray-300 hover:bg-gray-200">
                        <a target="_blank" href="{{path('app_space_show',{'id':space.id})}}">
                            <img class="block ml-auto mr-auto" src="https://placehold.it/150x150" alt="">
                            <h3 class="text-center font-black text-xl">{{space.name}}</h3>
                            <p class="text-center">{{space.description}}</p>
                        </a>
                    </div>
                {% endfor %}
                </div>
                <div class="flex justify-center hidden spaceLoader">
                    <div style="border-top-color:transparent" class="w-16 h-16 border-4 border-blue-400 border-solid rounded-full animate-spin">
                    </div>
                </div>
                <div class="flex justify-center" id="generateSpace">
                    <a href="#" class="mt-4 text-center rounded-full w-80 p-3 bg-indigo-500 font-bold text-white text-xl">Charger plus d'espaces</a>
                </div>
            </div>      
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ encore_entry_script_tags('space') }}
{% endblock %}